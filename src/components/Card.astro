---
interface Props {
	title: string;
	body: string;
	icons: string;
}

const { title, body, icons } = Astro.props;
---

<div class="flex flex-col bg-slate-300 border rounded p-4 dark:bg-slate-800 ">
  <h3 class="mb-2"><i class={icons}></i> {title}</h3>
  <p>{body}</p>
</div>